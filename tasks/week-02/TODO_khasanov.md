# TODO — Хасанов Дамир
**Модуль:** Ранжирование результатов
**Неделя:** 15–22 марта 2026
**Ветка:** `feature/khasanov/ranking-impl`

---

## Из week-01 должно быть готово
- `docs/RANKING.md` — критерии и веса алгоритма, примеры на бумаге
- `backend/ranking/ranking.py` — заглушка функции `rank_results(results, params)`

---

## Задача: реализовать алгоритм ранжирования

### Шаг 1 — Реализация алгоритма и тесты на него
Реализовать `backend/ranking/ranking.py`: `rank_results(results, params)` — скоринг по совпадению типа инструмента, бюджета, датчиков; возвращает топ-5 отсортированных. Написать тесты в `tests/test_ranking.py`: 10 объявлений на входе → правильные 5 на выходе, верный порядок сортировки.
`feat: implement ranking algorithm with tests`

### Шаг 2 — Интеграция в пайплайн и интеграционный тест
Подключить ранжирование в общий пайплайн: после `search_reverb()` вызывать `rank_results()`. Написать интеграционный тест на мок-данных Сидорова (`tests/mock_reverb.json`): запрос params → поиск → ранжирование → на выходе ≤ 5 результатов в правильном порядке.
`feat: integrate ranking into pipeline with integration test`

### Шаг 3 — Тесты на граничные случаи
Тесты в `tests/test_ranking.py`: пустой список на входе, все результаты с одинаковым скором, бюджет не указан в params, тип инструмента не совпадает ни с одним результатом.
`test: add ranking edge case tests`

---

> Стек: Python + FastAPI. Подробнее — [STACK.md](../../STACK.md)
