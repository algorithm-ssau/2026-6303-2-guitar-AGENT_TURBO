# TODO — Павлов Виктор

**Неделя 7:** 21–27 апреля 2026
**Ветка:** `feature/pavlov/prompt-quality-w7`

> Независимая задача. Все файлы — его собственные, никто другой в week-7 их не трогает.
> Можно мержить в любом порядке с другими участниками.

---

## Задача 1 (Docs): расширить MAPPING.md до 20 абстракций (PRD §5.2, §9)

### Что делать

- Переписать `docs/MAPPING.md` — расширить таблицу до **20 строк абстракций**. Формат таблицы тот же:
  `| Абстрактный эпитет | Категория / Деталь | Значение (ТТХ) | Примеры для search_queries | Примечания |`
- Добавить (в дополнение к существующим 6): **jazz**, **blues**, **funk**, **reggae**, **country**, **shoegaze**, **djent**, **grunge**, **lo-fi**, **sparkle**, **crunch**, **bell-like**, **honky**, **quack**, **chime**, **punchy**, **scooped**, **creamy**
- Для каждой строки — минимум 3 `search_queries` (бренд + модель)
- Сохранить исходный стиль: "Примечание" с 1–2 строками про физическую природу звука
- В конце файла добавить секцию `## Покрытие PRD` с пояснением, что это закрывает требование MVP §9 "маппинг 15–20 абстрактных характеристик"

### Файлы

- Изменить: `docs/MAPPING.md`

### Критерий приёмки

- В таблице ≥ 20 строк абстракций
- Каждая строка имеет минимум 3 search_queries
- Все примечания содержательны (не "todo")

### Тест: покрывается Задачей 4 (test_prompt_snapshot)

### Коммит: `docs: expand mapping to 20 abstractions (PRD §5.2)`

---

## Задача 2 (Backend): улучшить few-shot в build_search_prompt

### Что делать

- Обновить `backend/agent/param_extractor.py::build_search_prompt`:
  - Добавить **3 новых few-shot примера** в тело промпта:
    1. **Противоречивый запрос** (PRD сценарий 6): `"Хочу акустику с флойдом и EMG"` → валидный JSON с пояснением в `search_queries`, `type: "acoustic"` (противоречие уводится в consultation через mode_detector отдельно)
    2. **Бюджет в рублях**: `"ищу теле до 80 тыс руб"` → `price_max: 800` (курс 100 руб/$)
    3. **Vintage/год**: `"винтажный стратокастер 70-х до 3000$"` → `search_queries: ["Fender Stratocaster 1970s", "Fender Vintage Stratocaster"]`, `price_max: 3000`, `condition: "vintage"`
- Few-shot примеры оформить как JSON-блоки с комментариями-пояснениями
- **НЕ трогать** `llm_client.py`, `service.py`, `AGENT_PROMPT.md`

### Файлы

- Изменить: `backend/agent/param_extractor.py`

### Критерий приёмки

- В теле промпта видны 3 новых few-shot блока
- Сохраняется предыдущий формат (JSON на выходе)
- Запуск `pytest tests/test_param_extractor.py -v` зелёный

### Тест: покрывается Задачей 4

### Коммит: `feat: add few-shot examples for contradictions, rubles, vintage`

---

## Задача 3 (Docs): улучшить CONSULTATION_PROMPT.md

### Что делать

- Обновить `docs/CONSULTATION_PROMPT.md`:
  - Добавить ограничение длины ответа: "до 300 слов" в начале раздела правил
  - Добавить структурированный формат для сложных тем: markdown-списки, подзаголовки `###` для многоаспектных ответов
  - Добавить 2 few-shot примера в тело промпта:
    - Вопрос "что такое P90?" → структурированный ответ: Что это → Звучание → Где применяется
    - Вопрос "сингл или хамбакер для метала?" → сравнительный ответ с 2 столбцами плюсов/минусов
  - Явно запретить: списки гитар (это работа режима search), вывод цен, рекомендации конкретных моделей (передавать в search)

### Файлы

- Изменить: `docs/CONSULTATION_PROMPT.md`

### Критерий приёмки

- Промпт содержит ограничение "до 300 слов"
- Минимум 2 few-shot примера в теле
- Явные запреты прописаны в отдельной секции

### Тест: покрывается Задачей 5

### Коммит: `docs: improve consultation prompt with structure and constraints`

---

## Задача 4 (Test): snapshot-регрессия качества промпта поиска

### Что делать

- Создать `tests/fixtures/llm_snapshots.json` — 15 канонических запросов с ожидаемыми ответами замоканного LLM:
  ```json
  [
    {
      "query": "Найди стратокастер до 500$",
      "llm_response": {"search_queries": ["Fender Stratocaster", "Squier Classic Vibe Stratocaster"], "price_max": 500, "type": "stratocaster"},
      "expect": {"has_type": "stratocaster", "has_price_max": 500, "min_queries": 2}
    }
  ]
  ```
  Примеры должны покрывать: **strat/tele/LP, jazz/blues/metal, бюджет в $/руб, acoustic, vintage, противоречие, слэнг "страт/теле/лп"**
- Создать `tests/test_prompt_snapshot.py`:
  - Загрузить fixture
  - Замокать `LLMClient.extract_search_params` чтобы возвращал `llm_response` из fixture
  - Для каждого примера вызвать `extract_search_params` и проверить `expect`
  - Работает **без GROQ_API_KEY** (mock)
  - Жёсткий порог: минимум 13 из 15 проходят — иначе падает

### Файлы

- Создать: `tests/fixtures/llm_snapshots.json`
- Создать: `tests/test_prompt_snapshot.py`

### Критерий приёмки

- 15 запросов покрывают разнообразие сценариев
- Минимум 13 из 15 проходят (жёсткий assert)
- Тест стабилен между запусками
- Работает без GROQ_API_KEY

### Коммит: `test: add prompt snapshot regression tests for search params`

---

## Задача 5 (Test): регрессия качества консультационных ответов

### Что делать

- Создать `tests/test_consultation_quality.py` — 10 сценариев консультационных запросов:
  - "Что такое хамбакер?" → ожидание: ответ содержит слова "звукосниматель", "катушек", "2 катушки"
  - "Чем отличается стратокастер от телекастера?" → "мост", "звукосниматели", "конструкция"
  - "Какое дерево лучше для металла?" → "красное дерево", "махагон", "плотность"
  - "С чего начать новичку?" → "бюджет", "акустика", "электро" (одно из)
  - "P90 или хамбакер для блюза?" → "выход", "чистый звук", "мощность"
  - "Что такое floyd rose?" → "бридж", "тремоло", "строй"
  - "Какая мензура у стратокастера?" → "25.5", "дюйм"
  - "Как выбрать струны?" → "толщина", "гейдж", "натяжение"
  - "Что лучше: активные или пассивные датчики?" → "батарея", "выход", "перегруз"
  - "Зачем нужна полуакустика?" → "полый", "резонанс", "джаз"
- Замокать `LLMClient.ask` чтобы возвращал реалистичный ответ (fixture с 10 ответами)
- Проверка: каждый ответ содержит минимум 1 из ожидаемых ключевых слов
- Все тесты работают без GROQ_API_KEY

### Файлы

- Создать: `tests/test_consultation_quality.py`
- Создать: `tests/fixtures/consultation_answers.json`

### Критерий приёмки

- 10 сценариев проходят
- Fixture содержит реалистичные (не placeholder) ответы
- `pytest tests/test_consultation_quality.py -v` зелёный без API-ключа

### Коммит: `test: add consultation mode quality regression tests`
