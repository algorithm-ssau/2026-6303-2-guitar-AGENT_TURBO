# TODO — Хасанов Дамир

**Неделя 3:** 23–29 марта 2026
**Ветка:** `feature/khasanov/ranking-impl`

---

## Задача 1 (Backend): реализация алгоритма ранжирования

### Что делать

- Переписать `backend/ranking/ranking.py` — реализовать `rank_results()`:
  - Принимает: `results` (список объявлений), `params` (параметры поиска с search_queries, price_min, price_max)
  - Считает score для каждого результата по формулам из `docs/RANKING.md`
  - Упрощённая версия: бюджет (55%) + совпадение названия (45%)
  - Бюджет: в бюджете → 100, превышает до 20% → 50, больше → 0, бюджет не указан → 50
  - Название: полное совпадение фразы → 100, 2+ слова → 70, 1 слово → 40, нет → 0
  - Сортировка по score (убывание), возврат топ-5
  - Поле `_score` НЕ должно попадать в выходные данные

### Файлы

- Изменить: `backend/ranking/ranking.py`

### Критерий приёмки

- 10 результатов → 5 на выходе
- Гитара в бюджете ранжируется выше дорогой
- Совпадение по названию повышает позицию
- Пустой список → пустой результат
- `_score` не в output

### Тест: `tests/test_ranking.py`

- Минимум 5 тестов: топ-5, бюджет, название, пустой, _score

### Коммит: `feat: implement ranking algorithm with budget and title scoring`

---

## Задача 2 (Frontend): компонент подсказки бюджета

### Что делать

- Создать `BudgetHint.tsx`:
  - Props: `priceMin?: number`, `priceMax?: number`, `resultsCount: number`
  - Отображает: "Бюджет: до $500" / "$300 – $800" / "не ограничен" + количество результатов
- Добавить цветовые метки цены в `GuitarCard.tsx` (Павлова):
  - Зелёный: цена в бюджете (≤ priceMax)
  - Жёлтый: превышает до 20%
  - Красный: превышает более 20%

### Файлы

- Создать: `frontend/src/features/chat/components/BudgetHint.tsx`
- Изменить: `GuitarCard.tsx` — цветовая метка цены

### Критерий приёмки

- priceMax=500 → "до $500"
- priceMin=300, priceMax=800 → "$300 – $800"
- Без бюджета → "не ограничен"
- Цена окрашена в зелёный/жёлтый/красный

### Тест: `frontend/src/features/chat/__tests__/BudgetHint.test.tsx`

- 3 теста: priceMax, диапазон, без бюджета

### Коммит: `feat: add budget hint component with price indicators`

---

## Задача 3 (Тестирование): граничные случаи ранжирования

### Что делать

- Написать `tests/test_ranking_edge_cases.py`
- Минимум 7 тестов:
  - Без бюджета → нейтральный budget score
  - Один результат → возвращается как есть
  - Все цены выше бюджета → результаты всё равно возвращаются
  - Отрицательная цена → не ломается
  - `_score` не в output
  - Без search_queries → нейтральный title score
  - Точное совпадение фразы → максимальный score

### Файлы

- Создать: `tests/test_ranking_edge_cases.py`

### Критерий приёмки

- Все тесты проходят, алгоритм устойчив к крайним случаям

### Коммит: `test: add ranking edge case tests`
