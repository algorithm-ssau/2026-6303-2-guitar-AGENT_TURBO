# Router Prompt

Этот файл зарезервирован под описание правил LLM-router для маршрутизации между `search`, `clarification` и `consultation`.

Фактический prompt сейчас собирается в `backend/agent/llm_client.py`, чтобы держать JSON-контракт рядом с кодом вызова.
